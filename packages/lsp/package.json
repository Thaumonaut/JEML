{
  "name": "jotl-lsp",
  "version": "0.5.0",
  "description": "Language Server Protocol implementation for JOTL",
  "main": "dist/server.js",
  "bin": {
    "jotl-lsp": "./dist/server.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -p . --watch",
    "test": "node --test dist/test/*.test.js",
    "typecheck": "tsc -p . --noEmit",
    "dev": "tsc -p . --watch"
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.11"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
