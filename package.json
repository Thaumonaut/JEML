{
  "name": "jotl-monorepo",
  "version": "0.5.0",
  "private": true,
  "description": "JOTL — a structured markup language. Monorepo containing the compiler, LSP, Solid integration, and tooling.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev": "npm run dev -w jotl",
    "dev:lsp": "npm run dev -w jotl-lsp",
    "build:cli": "npm run build:cli -w jotl",
    "build:playground": "npm run build:playground -w jotl"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Thaumonaut/JOTL.git"
  },
  "license": "MIT"
}
